# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Built from content/blog by scripts/build-blog.mjs on every build and on dev
# server start — and the dev server's copy also carries drafts, which must not
# land in a commit. The markdown in content/blog is the source of truth.
src/data/blog.generated.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
todo.md

# Environment variables
.env
.env.example
